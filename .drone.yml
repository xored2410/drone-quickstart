---
kind: pipeline
type: docker
name: console

steps:
- name: hello from pipeline
  image: busybox
  commands:
  - echo truc truc
 
trigger:
  branch:
  - main
---
  kind: pipeline
  type: docker
  name: test

trigger:
  branch:
  - master

steps:
- name: clone
 image: node
 commands:
 - git clone https://github.com/xored2410/playwright-drone.git
 - npm install
 - npm run test:dev


# - name: playwright tests
#   image: node
#   commands:
#     - npm install
#     - npm run test:dev